bundle:
  name: ifood-data-eng-case

include:
  - resources/*.yml

targets:

  development:
    mode: development
    default: true
    workspace:
      host: https://dbc-2653231d-b327.cloud.databricks.com/
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}